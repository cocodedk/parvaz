# ── Go (core/) ──────────────────────────────────────────────────
*.exe
*.test
*.out
coverage.out
/bin/
/dist/
vendor/
/core/parvaz
/core/parvazd

# ── Android / Gradle (app/) ─────────────────────────────────────
.gradle/
build/
/app/build/
/app/release/
captures/
.cxx/
local.properties
*.keystore
*.jks
!release.keystore.gpg

# ── JniLibs bundled Go binaries (built per release) ─────────────
/app/src/main/jniLibs/*/libparvaz.so

# ── Editor / OS ─────────────────────────────────────────────────
.idea/
.vscode/
*.swp
.DS_Store

# ── Local secrets — never commit ────────────────────────────────
config.json
.env
.env.*
!.env.example

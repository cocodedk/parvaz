[0m
> build · kimi-k2.6
[0m
**Review: `core/cmd/parvazd/browser_live_test.go`**

- **L100-105**: The fix correctly surfaces the previously-discarded `io.ReadAll` error with `t.Fatalf`, preventing false passes on truncated streams. Ordering (read → close → check error) is correct — `resp.Body` is fully consumed into memory before close, so the close does not affect the error or the byte slice.
- **L108-111**: The `bytes.Contains` assertion is now gated behind the error check, eliminating the risk of matching `"cocode"` on a partial body when the underlying read failed. No nil-slice panic risk because `t.Fatalf` terminates execution before this line on error.

No other logic changes. The added lines are consistent with the commit intent.

**Verdict: clean**
